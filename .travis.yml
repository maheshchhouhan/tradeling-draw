language: node_js
node_js:
 - "node"
cache: npm

install:
  - npm install

script:
  - npm run test
  - npm run build

deploy:
  provider: pages
  skip-cleanup: true
  local_dir: build
  github-token: $GITHUB_TOKEN
  on:
    branch: MASTER